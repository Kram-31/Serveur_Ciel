services:
  nginx:
    image: nginx:latest
    container_name: nginx_proxy
    restart: always
    ports:
      - "80:80"  # Accès via http://IP_DU_SERVEUR
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - reseau_ciel

networks:
  reseau_ciel:
    external: true
